apiVersion: v1
kind: Service
metadata:
  name: {{ include "bilcool.fullname" . }}
  labels:
    {{- include "bilcool.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bilcool.selectorLabels" . | nindent 4 }}
