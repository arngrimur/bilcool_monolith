apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bilcool.fullname" . }}-test-connection"
  labels:
    {{- include "bilcool.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bilcool.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
