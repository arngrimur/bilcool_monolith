apiVersion: apps/v1
kind: Deployment
metadata:
  name: authentication
  labels:
    app: authentication
spec:
  replicas: 2
  selector:
    matchLabels:
      app: authentication
  template:
    metadata:
      name: authentication
      labels:
        app: authentication
    spec:
      initContainers:
        - name: wait-for-localstack
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until wget -qO- http://localstack:4566/_localstack/init/ready | grep -q SUCCESSFUL; do
                echo "Waiting for LocalStack init to complete..."; sleep 3;
              done
      containers:
        - name: authentication
          image: arngrimur/bilcool-authentication:{{ .Values.image.tag | default "latest" }}
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: bilcool-shared
            - configMapRef:
                name: authentication
            - secretRef:
                name: authentication
          livenessProbe:
            httpGet:
              path: /ping
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ping
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: authentication
spec:
  selector:
    app: authentication
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort
